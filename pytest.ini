[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: marks tests as slow (statistical, run with -m slow)
    conformance: marks tests as CAMARA spec conformance tests
    integration: marks tests requiring a running server
addopts = -v --tb=short
